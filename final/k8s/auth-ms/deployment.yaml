apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: auth-service
  template:
    metadata:
      labels:
        app: auth-service
    spec:
      nodeSelector:
        kubernetes.io/os: windows
      containers:
        - name: auth-service
          image: final-auth-ms:windows-latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
